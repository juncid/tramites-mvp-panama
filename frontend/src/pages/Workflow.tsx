import { Typography, Paper } from '@mui/material';

export const Workflow = () => {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Sistema de Workflow
      </Typography>
      <Typography color="text.secondary">
        Página de workflow en construcción...
      </Typography>
    </Paper>
  );
};
